/**
 * Feed route — placeholder for TanStack Router migration.
 *
 * Currently not imported — the FeedView is rendered inline in main.tsx.
 * When the Router migration is completed, this file will be the file-based
 * route entry for the Feed view.
 *
 * See Deferred to Follow-Up Work in the plan for migration context.
 */
export { FeedView as default } from '../../components/FeedView';
